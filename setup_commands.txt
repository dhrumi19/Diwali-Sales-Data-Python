# Create Django project
django-admin startproject data_analysis_dashboard .

# Create Django app
python manage.py startapp dashboard

# Install required packages
pip install django pandas numpy matplotlib seaborn scikit-learn pillow
